<form class="form-note">
<div align="center">  <img src="ruby.gif" alt="..." class="img-rounded" width="185" height="60"> 
  <br><br><br><br>
  <form class="navbar-form navbar-left" role="search">
  <div class="form-group">
    <input type="text" class="form-control" placeholder="Search">
  
<button type="submit" class="btn btn-default">Rechercher</button></div>

<br><br><br><br><br><br><br><br>

<a href="Inscription.html"> <button type="button" class="btn btn-default navbar-btn" data-toggle="modal">Inscription</button> </a>
<a href="Connexion.html"> <button type="button" class="btn btn-default navbar-btn" data-toggle="modal">Connexion</button> </a>
  
  </form>
</div>
  </form>


<h1>Mes vidéos</h1>

<table>
  <thead>
  </thead>

  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td><%= movie.movie_id %></td>
        <td><%= link_to 'Supprimer', movie, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>id</th>
      <th>url</th>
      <th>title</th>
    </tr>
  </thead>

  <tbody>
    <% @tests.each do |movie| %>
      <tr>
        <td><%= movie.id %></td>
        <td><%= movie.url %></td>
        <td><%= movie.title %></td>
      </tr>
    <% end %>
  </tbody> 
</table>

<br>

<%= link_to 'New Movie', new_movie_path %>
